# MongoDB connection (required). In production use Atlas or similar (e.g. mongodb+srv://...).
MONGODB_URI=mongodb://localhost:27017/plan-partner

# Auth (required in production): secret for signing JWTs. Use a long random string.
JWT_SECRET=your-secret-at-least-32-chars

# Optional: directory for uploaded files (default: ./uploads)
# UPLOAD_DIR=./uploads

# Optional: Google Gemini API key for "Analyze with AI" (meal photo) and chat/evidence
# Get one at https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=

PORT=3001

# Web Push (Solution 6). Generate with: node server/scripts/generate-vapid.cjs
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

# Optional: secret for internal cron. When set, the server auto-runs (no external cron needed):
#   - Routine pushes: every hour (UTC) — "Log BP/sugar now" at user's usual time
#   - Reminder escalations: daily at 09:00 UTC — Day 1→2→3→5 adaptive reminders
# Set in production so notifications run automatically.
# CRON_SECRET=
